#!/usr/bin/env bash

matugen image "$1" --source-color-index 2
kitty @ --to unix:/tmp/kitty set-colors -a "~/.config/kitty/colors.conf"
makoctl reload
pkill -USR2 waybar
tmux source-file ~/.config/tmux/tmux.conf
notify-send "Theme updated" "Applied colors from $1"
